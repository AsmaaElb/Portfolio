import React from "react";
import { SectionWrapper } from "../hoc";

const Feedbacks = () => {
  return null; // ou tu peux retourner <></> si tu veux garder un composant vide
};

export default SectionWrapper(Feedbacks, "");
